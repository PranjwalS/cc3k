export module util;

import <vector>;
import <string>;

template <typename T>
export std::string vtos(std::vector<T>& vec) {
    std::string res;

    for (int i = 0; i < vec.size(); i++) {
        
    }
}